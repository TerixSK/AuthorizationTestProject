name: authorization_test_project
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.15.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  hive: ^2.1.0
  hive_flutter: ^1.1.0
  equatable: ^2.0.3
  flutter_bloc: ^8.0.1
  get_it: ^7.2.0
  flutter_spinkit: ^5.1.0
  spring: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.4

  hive_generator: ^1.1.2
  build_runner: ^2.1.10

flutter:

  uses-material-design: true

  assets:
    - assets/images/profile.jpeg
    - assets/images/error.jpeg
